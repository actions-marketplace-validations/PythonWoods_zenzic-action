# SPDX-FileCopyrightText: 2026 PythonWoods <dev@pythonwoods.dev>
# SPDX-License-Identifier: Apache-2.0

[tool.bumpversion]
current_version = "2.14.0"
parse = "(?P<major>\\d+)\\.(?P<minor>\\d+)\\.(?P<patch>\\d+)"
serialize = ["{major}.{minor}.{patch}"]

[[tool.bumpversion.files]]
filename = "package.json"
search = '"version": "{current_version}"'
replace = '"version": "{new_version}"'

[[tool.bumpversion.files]]
filename = "RELEASE.md"
search = "v{current_version}"
replace = "v{new_version}"

[[tool.bumpversion.files]]
filename = "RELEASE.md"
search = '(?m)^(\| Date\s+\|\s+)\d{{4}}-\d{{2}}-\d{{2}}(\s*\|)$'
replace = '| Date    | {now:%Y-%m-%d} |'
regex = true

[[tool.bumpversion.files]]
filename = "CHANGELOG.md"
search = '## \[Unreleased\](\s*\n\*Upcoming changes for the next release\.\*)?'
replace = '''## [Unreleased]

*Upcoming changes for the next release.*

## [{new_version}] - {now:%Y-%m-%d}

Release notes for the `v{new_version}` release of `zenzic-action`.'''
regex = true

[[tool.bumpversion.files]]
filename = "RELEASE.md"
search = "- [ ] `package.json` version bumped to `{current_version}`"
replace = "- [ ] `package.json` version bumped to `{new_version}`"

[[tool.bumpversion.files]]
filename = "SECURITY.md"
search = '''| `{current_version}` (current) | ✅ All security fixes |
| `< {current_version}` | ❌ End of life — no support |'''
replace = '''| `{new_version}` (current) | ✅ All security fixes |
| `< {new_version}` | ❌ End of life — no support |'''

[[tool.bumpversion.files]]
filename = ".github/ISSUE_TEMPLATE/bug_report.yml"
search = 'placeholder: "v{current_version}"'
replace = 'placeholder: "v{new_version}"'

# ---------------------------------------------------------------------------
# Core pin — state tracking only.
# Actual updates are performed by `just pin-core <version>`.
# bump-my-version does NOT manage these files; they are listed here for discoverability.
# ---------------------------------------------------------------------------
[tool.bumpversion.custom_variables.core_version]
current = "0.30.0"
